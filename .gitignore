# OS metadata
.DS_Store
Thumbs.db

# Preview and screenshots (Optional, but usually ignored if repo is just code)
# preview.png

# Node (if used)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
